// test.js

function testAuthFunction() {
    const result = true;  // Substitua por algo que você esteja testando
    if (result === true) {
      console.log('Test passed: Auth function works');
    } else {
      console.error('Test failed: Auth function is broken');
    }
  }
  
  function testAnotherFunction() {
    const result = 5 + 5;  // Exemplo simples de cálculo
    if (result === 10) {
      console.log('Test passed: Addition works');
    } else {
      console.error('Test failed: Addition does not work');
    }
  }
  
  // Chama os testes
  testAuthFunction();
  testAnotherFunction();
  